services:  postgres:    container_name: db_postgresql    image: postgres    environment:      POSTGRES_USER: root      POSTGRES_PASSWORD: Aa@123      POSTGRES_DB: demo_db    ports:      - "5432:5432"    volumes:      - postgres_data:/var/lib/postgresql/data    networks:      - spring-boot-net  spring-boot-app:    container_name: spring-demo    image: docker-demo-spring:1.3.0    ports:      - "8080:8080"    networks:      - spring-boot-net    depends_on:      - postgresvolumes:  postgres_data: {}networks:  spring-boot-net:    driver: bridge